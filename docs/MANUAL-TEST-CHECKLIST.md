# Manual checklist location

The canonical Windows acceptance checklist is [MANUAL_TEST_CHECKLIST.md](MANUAL_TEST_CHECKLIST.md). Use that file so the recorded test fields, evidence paths, and `Pass`/`Fail`/`N/A` status stay synchronized. The reusable driver is `scripts/windows-interactive-acceptance.ps1`.
