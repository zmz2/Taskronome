# Taskronome 隐私说明

生效日期：2026-09-03

Taskronome 的设计原则是“只在本机工作”。应用不要求账号，不连接 Taskronome 自有服务器，也不集成广告、分析 SDK、崩溃遥测或云同步。

## 本机保存的数据

Taskronome 会在 `%LOCALAPPDATA%\Taskronome` 保存：

- 用户创建的任务名称、备注、顺序、时间片、启用和完成状态；
- 应用设置和窗口位置；
- 每个经过确认的实际工作段；
- 轮转、确认、暂停、跳过、完成和系统中断事件；
- 用于安全恢复的运行检查点；
- 本机诊断日志。

## Windows 功能

应用可调用 Windows 通知中心、系统提示音、任务栏闪烁、系统托盘、锁屏/睡眠/会话通知。这些调用用于提醒和防止错误计时，不会把任务内容上传给项目维护者。通知正文会在当前 Windows 用户的通知中心中显示，是否保留及保留多久由 Windows 设置决定。

## 删除与导出

用户可在设置页打开数据目录，备份或删除本地数据；删除前应先退出应用。统计页可以把工作段导出为用户指定位置的 CSV 文件。

卸载程序不会默认删除 `%LOCALAPPDATA%\Taskronome`，以避免意外丢失记录。用户可以在卸载后手动删除该目录。
