# Taskronome

**Give every task its turn.**

Taskronome is a Windows desktop application for strict round-robin time-slice task management. It is designed to keep multiple tasks moving in a deliberate sequence, with an in-app presence confirmation before each new time slice.

> Initial implementation is being prepared. See the repository issues for the complete development specification.
